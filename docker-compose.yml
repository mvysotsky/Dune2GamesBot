version: '3'

services:

  node:
    container_name: dune-bot-node
    build: ./docker/node
    networks:
      - dune-bot
    volumes:
      - ./app/:/usr/src/app
    command: ["sh", "-c", "docker-start ; pm2-runtime start /usr/src/app/server.js --watch"]

networks:
  dune-bot:
